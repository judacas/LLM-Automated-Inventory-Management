# Deploy Inventory MCP to Azure Functions (Demo)

This runbook deploys the Inventory MCP server to an existing Azure Function App **without containers**.

Note: This document is kept for reference. The project can also be hosted on App Service.

See the original content in repo history if needed, or update this runbook when Azure deployment is in scope.
