"""Admin orchestrator agent package.

This package contains lightweight agent-style routing logic for Contoso admins.

It is intentionally simple and deterministic so:
- unit tests can validate intent routing
- the orchestrator can call the Inventory MCP tools for real data
"""
